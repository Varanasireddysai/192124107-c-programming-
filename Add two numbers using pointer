#include<stdio.h>
int main()
{
  int a,b,*p,*q,c;
  
  printf("enter two integers to add/n");
  scanf("%d%d",&a,&b );
  p=&a;
  q=&b;
  c=*p + *q;
  printf("sum of the numbers=%d/n",c );
  
  return 0;
  
}
